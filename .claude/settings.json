{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "npm run test:e2e:ci || { printf '{\"continue\":false,\"stopReason\":\"E2E CI tests failed — fix failing tests before pushing.\"}'; exit 1; }",
            "timeout": 300,
            "statusMessage": "Running E2E CI tests before push..."
          }
        ]
      }
    ]
  }
}
